<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical">

    <include layout="@layout/nav_header_home" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_static_menu"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        style="@style/ItemListStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableStart="@drawable/ic_baseline_filter_list_24px"
        android:text="All Tasks"
        app:drawableTint="@color/colorPrimaryTextColor" />

    <androidx.recyclerview.widget.RecyclerView
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <include layout="@layout/divider" />

    <TextView
        style="@style/ItemListStyle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:drawableStart="@drawable/ic_add_24dp"
        android:text="@string/app_name"
        app:drawableTint="@color/colorPrimaryTextColor" />

</LinearLayout>