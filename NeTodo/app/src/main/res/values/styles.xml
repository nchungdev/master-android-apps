<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.GrayBackground" parent="AppTheme.NoActionBar">
        <item name="android:background">#FFFFFF</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="colorControlNormal">@color/colorSecondaryTextColor</item>
    </style>

    <style name="ToolbarPopupTheme" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarTheme" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ToolbarStyle">
        <item name="android:background">@android:color/transparent</item>
        <item name="popupTheme">@style/ToolbarPopupTheme</item>
        <item name="theme">@style/ToolbarTheme</item>
        <item name="titleTextColor">@color/colorSecondaryTextColor</item>
        <item name="navigationIcon">@drawable/ic_round_menu_24px</item>
    </style>

    <style name="BottomAppbarStyle">
        <item name="colorControlHighlight">@color/colorAccent</item>
    </style>

    <style name="ExpandedTitleStyle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textSize">45sp</item>
        <item name="android:textColor">@color/colorSecondaryTextColor</item>
        <item name="android:textStyle">bold</item>
        <item name="android:lines">2</item>
    </style>

    <style name="CollapsedTitleStyle" parent="@android:style/TextAppearance.Medium">
        <item name="android:textColor">@color/colorSecondaryTextColor</item>
    </style>

    <style name="CollapsedSubtitleStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Subtitle">
        <item name="android:textColor">@color/colorSecondaryTextColor</item>
    </style>

    <style name="ExpandedSubtitleStyle" parent="TextAppearance.AppCompat.Headline">
        <item name="android:textColor">@color/colorPrimaryTextColor</item>
        <item name="android:textSize">18sp</item>
    </style>

    <style name="ProgressBarStyle">
        <item name="android:colorAccent">@color/colorAccent</item>
    </style>
</resources>
